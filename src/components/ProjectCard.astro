---
import type { CollectionEntry } from "astro:content";

interface Props {
  project: CollectionEntry<"projects">;
  index: number;
}

const { project, index } = Astro.props;
const number = String(index + 1).padStart(2, "0");
---

<article class="project">
  <p class="number" aria-hidden="true">{number}</p>

  <div class="project-copy">
    {project.data.status && <p class="status">{project.data.status}</p>}
    <h3>{project.data.title}</h3>
    <p class="summary">{project.data.summary}</p>

    {
      project.data.links.length > 0 && (
        <div class="links">
          {project.data.links.map((link) => (
            <a href={link.url} target="_blank" rel="noreferrer">
              {link.label} <span aria-hidden="true">↗</span>
            </a>
          ))}
        </div>
      )
    }
  </div>

  <dl class="project-meta">
    {
      project.data.collaborators.length > 0 && (
        <div>
          <dt>With</dt>
          <dd>{project.data.collaborators.join(", ")}</dd>
        </div>
      )
    }
    {
      project.data.methods.length > 0 && (
        <div>
          <dt>Methods</dt>
          <dd>{project.data.methods.join(" · ")}</dd>
        </div>
      )
    }
  </dl>
</article>

<style>
  .project {
    display: grid;
    grid-template-columns: 2.25rem minmax(0, 1fr) minmax(13rem, 0.48fr);
    gap: clamp(1rem, 3vw, 2.5rem);
    padding: clamp(2rem, 5vw, 3.75rem) 0;
    border-top: 1px solid var(--color-border);
  }

  .number {
    margin: 0.25rem 0 0;
    color: var(--color-muted);
    font-size: 0.72rem;
    font-variant-numeric: tabular-nums;
    letter-spacing: 0.08em;
  }

  .status {
    margin: 0 0 0.7rem;
    color: var(--color-accent);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.075em;
    line-height: 1.4;
    text-transform: uppercase;
  }

  h3 {
    max-width: 46rem;
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.55rem, 2.8vw, 2.2rem);
    font-weight: 500;
    letter-spacing: -0.025em;
    line-height: 1.15;
  }

  .summary {
    max-width: 43rem;
    margin: 1rem 0 0;
    color: var(--color-muted);
    font-size: 0.95rem;
    line-height: 1.75;
  }

  .links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem 1.25rem;
    margin-top: 1.1rem;
  }

  .links a {
    color: var(--color-text);
    font-size: 0.86rem;
    font-weight: 600;
    text-underline-offset: 0.27em;
  }

  .links a:hover {
    color: var(--color-accent);
  }

  .project-meta {
    display: grid;
    align-content: start;
    gap: 1.35rem;
    margin: 0;
    padding-top: 0.15rem;
  }

  .project-meta div {
    display: grid;
    gap: 0.35rem;
  }

  dt {
    color: var(--color-muted);
    font-size: 0.69rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  dd {
    margin: 0;
    color: var(--color-text);
    font-size: 0.82rem;
    line-height: 1.65;
  }

  @media (max-width: 780px) {
    .project {
      grid-template-columns: 1.75rem minmax(0, 1fr);
    }

    .project-meta {
      grid-column: 2;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 520px) {
    .project {
      grid-template-columns: 1fr;
    }

    .number,
    .project-meta {
      grid-column: 1;
    }

    .number {
      margin: 0;
    }

    .project-meta {
      grid-template-columns: 1fr;
    }
  }
</style>
