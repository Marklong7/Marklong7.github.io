---
const pathname = Astro.url.pathname;
const navItems = [
  { label: "Home", href: "/", active: pathname === "/" },
  { label: "Research", href: "/research/", active: pathname.startsWith("/research") },
  { label: "Talk", href: "/talks/", active: pathname.startsWith("/talks") },
  { label: "Blog", href: "/blog/", active: pathname.startsWith("/blog") },
];
---

<header class="site-header">
  <div class="header-inner shell">
    <a class="wordmark" href="/" aria-label="Jialong (Mark) Li, home">Jialong (Mark) Li</a>

    <nav aria-label="Primary navigation">
      <ul>
        {
          navItems.map((item) => (
            <li>
              <a href={item.href} aria-current={item.active ? "page" : undefined}>
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid var(--color-border);
    background: var(--color-background);
  }

  .header-inner {
    display: flex;
    min-height: 4.75rem;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .wordmark {
    font-family: var(--font-body);
    font-size: var(--text-heading);
    font-weight: 600;
    letter-spacing: -0.018em;
    line-height: 1;
    text-decoration: none;
  }

  ul {
    display: flex;
    margin: 0;
    padding: 0;
    list-style: none;
    flex-wrap: wrap;
    gap: clamp(1.1rem, 3vw, 2.5rem);
  }

  nav a {
    position: relative;
    display: inline-block;
    padding: 0.25rem 0;
    color: var(--color-muted);
    font-size: var(--text-body);
    font-weight: 600;
    text-decoration: none;
  }

  nav a:hover,
  nav a[aria-current="page"] {
    color: var(--color-link);
  }

  nav a[aria-current="page"]::after {
    position: absolute;
    right: 0;
    bottom: -0.22rem;
    left: 0;
    height: 2px;
    background: var(--color-accent);
    content: "";
  }

  @media (max-width: 680px) {
    .header-inner {
      min-height: 0;
      align-items: flex-start;
      flex-direction: column;
      gap: 0.8rem;
      padding-block: 1rem;
    }

    ul {
      gap: 0.9rem;
    }

  }
</style>
